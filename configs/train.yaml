dataset_config: configs/dataset.yaml
manifest_file: data/split_manifest.json

model:
  name: rfdetr_small
  pretrained_checkpoint: rf-detr-small.pth

training:
  device: cuda
  seed: 42
  batch_size: 4
  grad_accum_steps: 4
  epochs: 30
  lr: 0.0001
  use_ema: true
  early_stopping: true
